//1//
function multiply(a, b){
  return a * b
}
//4//
function opposite(number) {
  return -number
}
//3//
function makeNegative(num) {
    if (num === 0 || num < 0) {
        return num;
    } else {
        return -num;
    }
}